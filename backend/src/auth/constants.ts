export const IS_PUBLIC_KEY = 'auth:isPublic';

/** После проверки JWT — id пользователя в БД */
export const REQUEST_USER_ID = 'authUserId';

export const ACCESS_TOKEN_COOKIE = 'quizoo_access_token';
