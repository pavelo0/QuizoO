/**
 * Точка входа приложения — `src/main.tsx` рендерит TanStack Router, не этот файл.
 * Для проверки UI Kit открой главную страницу (`/`) в dev-сервере.
 */
function App() {
  return null;
}

export default App;
